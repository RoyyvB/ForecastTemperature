# This file serves to store the parameters for each model in.
# It has its own separate file to prevent clutter in the scripts.

rf_params = {
    'n_estimators':1000,
    'criterion':'mse',
    'max_depth': None,
}